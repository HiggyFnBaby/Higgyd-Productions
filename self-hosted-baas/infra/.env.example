# Copy to .env and fill in real values. Never commit .env.

# Postgres — shared instance for app data, Gitea's DB, and pgvector storage.
POSTGRES_USER=baas_admin
POSTGRES_PASSWORD=change-me
POSTGRES_DB=app_data

# Separate logical database on the same Postgres instance, for Gitea.
GITEA_DB_NAME=gitea
