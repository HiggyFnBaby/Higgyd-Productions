# Todo

A minimalist, single-file todo list. Check an item off and it strikes through and drops to the bottom of the list; uncheck to bring it back. Tasks persist in the browser via `localStorage`.

## Run it

Open `index.html` directly in a browser, or serve the folder:

```
npx serve todo-app
```
